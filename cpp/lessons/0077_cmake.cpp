// Lesson: CMake - CMakeLists.txt, add_executable, target_sources
// Build: Copy 0077_CMakeLists.txt to CMakeLists.txt in this dir, then:
//   mkdir -p build && cd build && cmake .. && cmake --build .

#include <iostream>

int main() {
    std::cout << "Built with CMake!\n";
    return 0;
}
